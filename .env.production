# Public client env for production web builds (Vercel / expo export).
# No secrets — anon keys can be added in the Vercel dashboard if needed.

EXPO_PUBLIC_API_BASE_URL=https://api-production-5cea.up.railway.app
EXPO_PUBLIC_APP_ENV=production
EXPO_PUBLIC_AI_PROVIDER=mock
EXPO_PUBLIC_USE_RN_FETCH=true
